{
  "name": "hnpwa-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest",
    "build": "npm run build --workspaces",
    "clean": "rm -rf node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^1.0.0"
  }
}
